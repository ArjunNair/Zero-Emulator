<Project Sdk="Microsoft.NET.Sdk">
  <!-- Avalonia.Headless.XUnit 12 is built against xunit v3, so this project uses v3 (the core tests stay on v2). -->
  <PropertyGroup>
    <TargetFramework>$(ZeroTargetFramework)</TargetFramework>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <RootNamespace>Zero.App.Tests</RootNamespace>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- Mirror the runtime knobs from Zero.App so the tests run in the same mode the app does.
         Without this, a theme that trips over globalization-invariant mode passes here and
         crashes on start-up in the real app. -->
    <InvariantGlobalization>true</InvariantGlobalization>
    <PredefinedCulturesOnly>false</PredefinedCulturesOnly>
    <OutputType>Exe</OutputType>
    <TestingPlatformDotnetTestSupport>true</TestingPlatformDotnetTestSupport>
    <UseMicrosoftTestingPlatformRunner>true</UseMicrosoftTestingPlatformRunner>
  </PropertyGroup>
  <ItemGroup>
        <PackageReference Include="xunit.v3" Version="3.2.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.5" PrivateAssets="all" />
    <PackageReference Include="Avalonia.Headless.XUnit" Version="12.1.2" />
    <PackageReference Include="Avalonia.Skia" Version="12.1.2" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Zero.App\Zero.App.csproj" />
    <ProjectReference Include="..\Zero.TestSupport\Zero.TestSupport.csproj" />
  </ItemGroup>
</Project>
